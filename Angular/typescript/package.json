{
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.2"
  },
  "scripts": {
    "start": "npx tsc && node build/generics.ts",
    "start:dev": "ts-node-dev --respawn --transpile-only src/index.ts"
  }
}
